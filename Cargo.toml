[package]
name = "reinze-lib-util"
version = "0.1.0"
edition = "2018"

[dependencies]
format_num = "0.1.0"
meval = { git = "https://github.com/ryanwohara/meval-rs.git", branch = "main" }
regex = "1.10.2"

[lib]
crate-type = ["dylib"]
